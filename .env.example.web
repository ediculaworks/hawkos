# ── Hawk OS — Web Dashboard Environment ──────────────────────────────────────
# The web container only needs non-tenant-specific vars.
# Tenant-specific config (Supabase URLs, agent ports) comes from tenants.json.

NODE_ENV=production

# Fallback for single-tenant mode (optional if using tenants.json)
# NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
# NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJ...
# NEXT_PUBLIC_AGENT_API_URL=http://localhost:3001
# NEXT_PUBLIC_AGENT_API_TOKEN=your-token
